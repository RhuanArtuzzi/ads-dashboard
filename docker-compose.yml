# Docker Stack para: docker stack deploy -c docker-compose.yml ads-dashboard
# Imagens devem ser buildadas antes do deploy (ver deploy.sh)

services:
  backend:
    image: ominy-ads-backend:latest
    environment:
      DATABASE_URL: postgresql://ominy:${POSTGRES_PASSWORD}@postgres_postgres:5432/ominy_ads?connection_limit=5&pool_timeout=10&connect_timeout=10
      REDIS_URL: redis://redis_redis:6379
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      JWT_SECRET: ${JWT_SECRET}
      PORT: "8000"
      NODE_ENV: production
    volumes:
      - /opt/ads-dashboard/config:/app/config
    networks:
      - ominy-network
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == vmi3133382
      restart_policy:
        condition: on-failure
        delay: 10s
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.ads-api.rule=Host(`api-dashboard.ominy.tec.br`)"
        - "traefik.http.routers.ads-api.entrypoints=websecure"
        - "traefik.http.routers.ads-api.tls=true"
        - "traefik.http.routers.ads-api.tls.certresolver=letsencrypt"
        - "traefik.http.services.ads-api.loadbalancer.server.port=8000"
        - "traefik.docker.network=ominy-network"

  frontend:
    image: ominy-ads-frontend:latest
    networks:
      - ominy-network
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname == vmi3133382
      restart_policy:
        condition: on-failure
        delay: 10s
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.ads-frontend.rule=Host(`dashboard.ominy.tec.br`)"
        - "traefik.http.routers.ads-frontend.entrypoints=websecure"
        - "traefik.http.routers.ads-frontend.tls=true"
        - "traefik.http.routers.ads-frontend.tls.certresolver=letsencrypt"
        - "traefik.http.services.ads-frontend.loadbalancer.server.port=3000"
        - "traefik.docker.network=ominy-network"

networks:
  ominy-network:
    external: true
